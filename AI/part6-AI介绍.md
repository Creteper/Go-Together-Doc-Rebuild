
各位评委老师好！
我是团队中的AI开发工程师，[PPT切换]
主要负责模型训练,语音交互，视觉识别以及硬件开发。[PPT切换]

在我们平时使用AI使会经常遇到许多问题 例如
[PPT切换]
“当我们对着AI问：‘**故宫的角楼为什么修得这么尖/这么复杂？**’

结果AI回答说：**‘因为皇帝梦见了凤凰，角楼是仿照凤凰的尾羽修建的。’**

这个回答听起来很美，但完全是AI编造的。历史上角楼是为了军事防御和彰显皇权而建，和凤凰的梦没关系。  
这就是典型的‘AI幻觉’——它不是在检索事实，而是在‘创作’一个符合语境的故事。  
**当虚构的故事取代了真实的历史，我们的文化记忆就在被悄悄篡改和杀死。**”
[PPT切换]
 “因此，我们转向AI本身去寻找答案。今天，我将向大家展示，如何**利用AI最前沿的技术，来约束和纠正小游 同学的幻觉问题。”
他有三大超能力：[PPT切换]
景点故事的讲解、精准路径的规划、以及像朋友一样实时问答。

[PPT切换]
这些能力背后，
是三个技术引擎：
大语言模型、语音交互、视觉识别。[PPT切换]
但最初，
我们在开发业务时被困在AI的“黑暗森林”——模型会编造不存在的历史，数据永远滞后于现实。
[PPT切换]
直到我们找到光：**RAG向量数据库**。
它像给AI配了个实时百科全书——所有回答都有据可依！
[PPT切换]
我们选用了BAAI和中科大的‘模型：
它能读懂百种语言，
拥有8k的tokens，
[PPT切换]
但是传统RAG从向量数据库检索到的原始数据往往包含大量无关符号和冗余文本
**
为此[PPT切换]
我们开发了RAG PRO 架构
我们使用自训练的轻量级模型对检索结果进行预处理，
将混乱文本转化为结构化自然语言，
并结合用户当前位置天气和时间等上下文信息，
再交由DeepSeek R1模型进行深度思考，最终生成准确、真实     且个性化的旅行规划。
[PPT切换]
但户外才是终极考场！
在拥挤的天坛，传统AI常把祈年殿认成“普通圆楼”。
[PPT切换]
我们自研了**“Phantom Eye”视觉系统**：
[PPT切换]
第一层：OpenCV增强图像，像给眼睛戴“智能滤镜”；
第二层：YOLO精准定位建筑；
第三层：通过视觉语言模型进行深度语义理解。

解决了视觉问题后,还需要解决模型难以在小型设备上运行的问题

所以我们再转为NCNN格式提高兼容性缩小体积

现在，即使是在移动端也可以使用我们的模型

[PPT切换]
那么现在，让我们了解一下这些超能力是如何进行运作的

当用户提问时，如果数据量不够 AI 责会直接编故事，
而我们的系统会这样工作

> 跳转split_text_into_chunks()
>
> 我们先进行了文档的切块，把故宫的史料变成AI可以消化的”知识胶囊“
> 而在此刻您看到的不是函数，而是AI的”防幻觉安全阀“

在这之后我们会将文字转变为向量
> 跳转get_embedding()
>
> 在这里我们让数据变成AI能够理解的向量,让AI”懂“得您问的是建筑排水，而不是神话传说

 接着使用同文件的calculate_similarity(尻Q累特，赛没拉瑞题)
> 跳转calculate_similarity()
> 
> 这个函数用来计算 查询问题 和 数据库文档块 之间的相似度

> 然后与 extract_keywords 进行互相配合

> 提取关键词还有词频统计和词性分类

而在这里则是让AI有据可依的关键
> 跳转search_documents() (RAG SERVICE 文件下)
> 在这里，彻底杀死幻觉
> 我们先计算了相似度，
>
> 只返回大于指定的相似度的结果 这里使用0.3
>
> 这一步就是为了让AI拒接编造
>
> 阈值0.3不是魔法数字，而是上百次测试中的测出来的”真实与编造“的分界线

> 在最后我们需要用到_process_rag 这个异步函数
> 在从RAG服务器获取数据后，序列化数据，再利用AI进行数据预处理，
> 最后会将它转化为结构清晰的自然语言

在这些步骤之后我们将
RAG增强索引与
实时天气获取
还有大语言模型等
多个关键模块
整合到了一个
函数也就是api函数,他构成了我们最重要的AI路径规划功能.

而当用户开口提问时小
游同学的耳朵是怎样构成的呢?
> 跳转 Voice Be Clear 实现主要函数() 《实际的VBC代码函数》
>[PPT切换]
> 这里就是我们的VBC技术
> 先用FFMPEG过滤底噪，再通过频谱分析提纯人声
> 然后应用专用音频AI模型二次过滤，然后我们使用submit_task() 函数 结合火山引擎的专业音频处理能力 将语音转换为文字
> 这样就可以让AI在广场舞中也可以听清用户的声音
>[PPT切换]
> 光听还不够 我们还要让小游能够讲的也清晰
> 在这里我们使用text_to_speech_async()
> 实现的文本转语音的功能，并且还能够对长文本进行自动分割处理
> 最后，get_guide() 函数用于生成我们的 AI 导游解说内容.

[PPT切换]

那么接下来就请看我们的真实效果
我现在进行启动我的各个后端服务
分别是RAG服务和后端服务,
可以看到都已经启动成功了  分别挂载在5000和8000端口
向量索引模型在GPU云服务器上也能正常链接
为了方便管理，我们做了一个原创的功能后台页面，大家请看我的屏幕

打开我们本地的8000端口，
进入数据库页面
先让我们上传一个知识库文件去对他进行切块和关键词的生成
这里就是用到了我们上面提到的分档切块

进入数据库页面，查看我们刚刚上传的文件详情
我们可以直观的看到我们的关键词统计，索引，
和文本块

接下来我们进行测试，来直观的感受我们的RAG功能，
我们打出一个问题，发送。
就可以看到那些超过相似度阈值数据被一一排列出来
通过文本分块与关键词索引，彻底告别模型幻觉与消息滞后。

接下来让我们到LLM界面去进行测试全部流程
我们首先通过语音交互进行输入我们要生成的景点位置和想要的规划
他会先VBC降噪，通过 FFMPEG 进行音频预处理 
频谱分析和噪音检测进行第一次过滤
再通过AI智能滤波处理音频 
最后通过 音频复杂程度选择使用
本地VOSK还是云端+本地混合识别
在识别成功后，会将结果显示在文本框内

接着让我们为行程规划与景点介绍(原导游功能)同时发送一个任务，(北京一日游 两三个景点就行)

我们先看景点介绍
[PPT切换]
景点介绍功能通过语义分析，选择了DS3.5这个快速模型
然后在RAG数据库中搜索到了北京故宫的相关信息，
并且通过AI智能分析进行处理，得出简洁的信息,

最后可以看到我们生成出的导游文案和音频 我们来听一下是否和文案一致

再来看一下路径规划测试

而这里自动选择了DeepSeek R1模型，适合复杂推理以及深度规划

运行流程与景点介绍流程一样，

##### 通过RAG去进行索引，再用RAGPRO生成自然语言

不同的是，这里使用了动态的天气获取，辅助了路线的规划

而这里则可以看到思考过程，
结束后他会生成Json结构的旅游规划,
里面包含了所有的行程
[PPT切换]
以上就是我们整个软件上的核心功能

包括
云端语音生成 
本地+云端 语音识别 
大语言模型生成 
Rag增强数据库索引 
VBC降噪
[PPT切换]
除了这些功能 我们还有机器人使用的视觉识别模块 

我来简单展示一下机器人的视觉识别

展示YOLO  让机器人更好的理解它看到的东西

[PPT切换]
除了以上核心技术，我们还使用了：
LM Studio：运行向量索引模型
Flask：构建轻量后端服务
FastAPI：应对高并发的高效后端

[PPT切换]

**最让我骄傲的，是“小身材大智慧”**：
通过模型蒸馏和量化，我们把体积压缩2/3，却保留95%准确率。这意味着：
**文化，终于不再被算力囚禁**。

接下来我将会为各位展示我们的模型轻量化技术

(展示一下数据集)

这是我们用来训练文旅垂直领域模型的训练集

我们使用Pytorch-CUDA对模型蒸馏+量化并且微调

一套组合拳大家可以看一下我们模型大小变化
我们的模型体积减少了三分之二

(展示性能测试)
然后看一下我们的模型性能测试

我们可以看出在体积减少三分之二的情况下，模型的准确率几乎没变化
这就是我们模型轻量化技术的功劳!

最后是YOLO (展示视觉识别) 测试鼠标和人是否能够检测 OK，没有问题[PPT切换 - 83]
现在把时间交给前端工程师，让他来为大家展示我们的项目。
