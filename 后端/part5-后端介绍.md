## 后端

接下来将有我为大家展示我们的后端服务的各个功能
我在整个项目开发过程中，
[PPT切换 - 41]
我全程负责业务逻辑、数据库设计，以及数据的爬取、清洗与标注。[PPT切换 - 42]

在业务逻辑和数据的爬取工作中我选择了rust这个天生就杜绝了内存泄漏的语言
这样即使系统运行100天，您的数据依然坚如磐石[PPT切换 - 43]

搭配Actix这个rust的原生框架，
响应速度比行业平均快3倍--
即使您在长城烽火台，路线也能秒级加载[PPT切换 - 44]
当数据量冲到51万行时，我们果断从SQLite升级到MySQL。+
为什么？因为51万条真实路径不是冰冷数字，而是51万个帮您找到最佳路线的机会。[PPT切换 - 45]

我们的数据库就像一座精心设计的博物馆[PPT切换 - 46]
-   users表是接待处，确保了每位游客可以安全出入[PPT切换 - 47]
-   book_mark表是您的私人笔记，保存您的心动时刻[PPT切换 - 48]
-   gd_data表使展品目录，记录了每个景点的详细信息[PPT切换 - 49]
-   而route_points表--是这座博物馆的心脏，跳动着51万条真实路径。
[PPT切换 - 50]
这个心脏的每一次的搏动，都源于鲜活的数据血液[PPT切换 - 51]
正是我亲手设计的爬虫，为它持续进行供氧。
为了获取最新地图数据，我打造了专属爬虫：[PPT切换 - 52]
只用reqwest这把‘轻量武器’，舍弃臃肿框架—— 精准、高效，速度提升40%！
更重要的是，我们像考古学家一样：[PPT切换 - 53]
严格遵循Robots协议，只挖掘地图商开放的宝藏，
让每条数据都合法合规，安全抵达您的指尖。”**[PPT切换 - 54]

### 更改


我们始终认为，一切智能的根源都离不开数据
所以这里将会展示我们的数据是如何获取并且展示在大家的面前的

首先我先获取了所有城市的城市ID
(进入fetch_city_code函数)
利用SQL语句中的去重语句并且将它们组合成数组并将其返回
而后我们根据获取到的城市ID进行对应城市景点ID的获取

	这里的查询内容相当于SQL语句中的 从gd_data表查询city_code = 各个城市的city_code 

最初，我们以为485个景点只是485个点，
直到我们在任意两点之间进行排列计算时才发现：485×484÷2=117,370对关系对 -- 
像一场必须完美配对的旅行‘相亲会
这意味着，每个景点都要与其他484个景点建立联系，
而关键就在于我们的generate_pairs函数内的断点传续与双循环算法的互相配合

在起初，
我们卡在了关键问题：如何确保每对只计算一次，且断网后不重头开始？
直到一天，我在小学操场看到孩子们玩‘丢手绢’：
第一个孩子只把手绢丢给后面的人，
第二个孩子跳过第一个，只丢给更后面的人……
瞬间我懂了：这不就是最自然的‘不重复配对’吗？
于是，我们让系统成了‘会记忆的孩子’：

而我们的这里的断点传续——就能让孩子记住‘我刚丢到第7个同学’，回来后继续游戏；
而外面的匿名函数是‘记录老师’，
每生成一对就实时存档，
用Box::pin锁住内存，杜绝数据丢失。

接下来
让我们以北京为例，
亲眼见证数据如何变成旅行体验的

首先我们需要查询北京城市的编码表（021），以及我们的POI类型表
这里可以看到北京的poi类型表是021,而我们风景类poi是110000，
那么现在就可以完善我们的爬虫代码了，
首先我们需要构建我们事先封装好的ReqwestGd类
城市的adcode是021,
搜索的类型是110000风景名胜，
关键词就是北京，



现在让我们访问一下我们的网站，
可以看到我们的地图标点是没有得

现在我们进行运行，由于修改了代码所以构建会慢一点
我们的程序并没有结果而是执行到了程序的最底端，
我初步怀疑是代码逻辑出了异常，我们返回代码看一眼，
看一下我们的执行函数begin_gd_data()
--// 讲述底层使用async，所以上层也要用async，但是我们没有用awiat去等待
为了爬取速度更快，我们的底层用了async异步去执行
而相应的我们这里的函数也要使用async去执行，
但是这里并没有报错，可能是我们执行时出了问题，
让我们回到执行，

而在这里我发现了我们的错误，
因为没有在执行时使用await去等待函数执行，
所以程序执行到了最底端，
我们添加上后再次执行一下，应该就可以看到返回结果了
现在我们再执行一下



可以看到我们的数据已经返回了，
现在我们去前端刷新一下，
地图标点正常，
随着刷新次数的增多，标记点也会越来越多
现在让我们爬取一下两个点的路径，
在有了结果后进行一下测试，
随机挑两个，
进行测试

可以看到路径也是正常返回,
用APIFOX进行测试也可以看到我们的经纬路径返回与我们的数据库内的一致

这里密密麻麻的是数据并不是终点
而是AI理解旅行的起点。

当小游同学告诉您：
"中和殿此刻游客较少"，
背后就是这117,370条路径的智慧。

而这就是后端的价值——
让技术隐形，让体验闪光。[PPT切换 - 55]

接下来，请我们的AI开发工程师，
为您展示这些数据如何变成有温度的旅行伙伴。[PPT切换 - 56]

【21分】
